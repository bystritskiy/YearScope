{
  "name": "yearscope",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Dashboard: how much of the year went to games, TV shows, movies, books and workouts",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.ts",
    "demo": "node src/server.ts --demo",
    "dev": "node --watch --env-file-if-exists=.env src/server.ts",
    "sync": "node --env-file-if-exists=.env src/cli-sync.ts"
  },
  "license": "MIT"
}
